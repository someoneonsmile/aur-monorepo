# 复制这份文件为 .env，然后按自己的情况改：
#   cp .env.example .env
#
# justfile 里开了 `set dotenv-load := true`，.env 会被自动加载，
# 不用每次手动 export。真正的 .env 千万别提交进仓库（见下面 .gitignore 建议）。

# ---- pacman 国内镜像源，二选一，都不填就用 justfile 里写死的清华源默认值 ----

# 方式一：直接指定一个镜像源 URL（$repo、$arch 是 pacman 自己认的占位符，
# 不是要你填具体值，原样保留即可）
# PACMAN_MIRROR='https://mirrors.tuna.tsinghua.edu.cn/archlinux/$repo/os/$arch'
# 其它可选源：
# PACMAN_MIRROR='https://mirrors.ustc.edu.cn/archlinux/$repo/os/$arch'
# PACMAN_MIRROR='https://mirrors.aliyun.com/archlinux/$repo/os/$arch'

# 方式二：复用宿主机上已经测速选好的 mirrorlist 文件（比如你本机就是 Arch），
# 优先级比 PACMAN_MIRROR 高
# LOCAL_MIRRORLIST=/etc/pacman.d/mirrorlist


# ---- 推送 AUR 需要的 SSH 私钥路径 ----
# 对应你在 AUR 账号里配置的公钥所对应的私钥文件
# AUR_SSH_KEY_PATH=/home/you/.ssh/aur_ed25519


# ---- gh issue 通知需要（只在想本地测试失败通知那段逻辑时才需要设）----
# 一个有当前仓库 issues:write 权限的 token，本地一般用不到，
# CI 里已经用 secrets.GITHUB_TOKEN 自动注入，不需要在这里填
# GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

